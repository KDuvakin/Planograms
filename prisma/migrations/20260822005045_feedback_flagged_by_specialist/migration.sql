BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Feedback] ADD [flaggedBySpecialist] BIT NOT NULL CONSTRAINT [Feedback_flaggedBySpecialist_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW;

END CATCH;
